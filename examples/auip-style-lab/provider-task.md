在当前隔离工作目录里制作一个精致、可离线运行的三圆盘汉诺塔应用，中文界面。
这是 Amadeus artifact 视觉语言的 Provider 迁移实验；以当前 DESIGN.md 版本为准。

先完整阅读 DESIGN.md，再使用 base.css。保持这两个输入文件原样；在你自己的
app.css 中实现领域布局。只允许使用当前工作目录里的设计材料，不要搜索父仓库
的参考 HTML 或其他实验样例。你没有固定页面模板，布局由汉诺塔的玩法决定。

视觉目标：外部操作区属于 Amadeus 的安静实验室终端；中央棋盘使用深灰绿、低饱和
中性色，盘片保留低饱和领域色，小面积暖色作为点缀。围绕棋盘组织交互，别把页面变成卡片仪表盘。内容不得重复画
Host 窗口标题栏或假连接状态。390px 和 1280px 都应清晰且可操作。

功能：3 个圆盘从 A 柱移到 C 柱；一次移动一个顶端圆盘，大盘不能放小盘上。
用户通过点击或键盘选择源柱和目标柱来完成移动；显示当前步数、规则和真实反馈。
重置可用；完成后明确显示成功，仍能重新玩。拒绝非法移动时不改棋盘和步数。
不要添加自动求解或提示系统，专注核心玩法和视觉品质。

接入 AUIP，支持 Amadeus 通过低频、受限的 move 动作参与真正的移盘核心循环，
并能读取完整棋盘与合法选项。依照 Host 提供的 auip-authoring skill 和公开接口，
保持原始 MCP payload 粒度；不要把整个解题过程合并为 solve 动作。
应用在未 Attach 时仍独立可玩，不伪造 Amadeus 连接、参与者或动作事实。
完成 manifest validate / sync / 隔离真实 entry preflight。不要启动桌面应用、不要连接
生产 AUIP server、不要修改 SDK、不要安装依赖或提交 git。

最终交付 index.html、app.css（如需要）、app.js（如需要）、auip.manifest.json、
所需本地资源和 AUTHOR_REPORT.md。报告说明布局取舍、核心动作覆盖、实际通过的
检查与未验证之处。生成并检查结果后结束，保留全部源码，供实验主持者独立评价。
